[tool.pytest.ini_options]
asyncio_mode = "strict"
addopts = "-m 'not integration'"
markers = [
    "integration: real network tests against video platforms",
    "slow: tests that require a full video download (tiktok, dailymotion)",
]
